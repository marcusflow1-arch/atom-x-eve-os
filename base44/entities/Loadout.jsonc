{
  "name": "Loadout",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string",
      "description": "Owner of this loadout"
    },
    "name": {
      "type": "string",
      "description": "Loadout name"
    },
    "description": {
      "type": "string",
      "description": "Loadout description"
    },
    "loadout_type": {
      "type": "string",
      "enum": [
        "equipment",
        "skills"
      ],
      "default": "equipment",
      "description": "Whether this is a gear loadout or the Luna Skill Book hotbar"
    },
    "game_id": {
      "type": "string",
      "description": "Optional game this loadout is for"
    },
    "genre": {
      "type": "string",
      "description": "Genre this loadout targets"
    },
    "equipped_items": {
      "type": "object",
      "description": "Mapping of equipment slots to item IDs"
    },
    "skill_slots": {
      "type": "object",
      "default": {},
      "description": "Persistent four-slot Luna Skill Book mapping. Keys 0-3 map to owned UserCard IDs."
    },
    "skill_set_id": {
      "type": "string",
      "description": "Stable identifier for one of the three Luna dashboard skill/genre rows."
    },
    "skill_set_name": {
      "type": "string",
      "description": "Player-facing name for the Luna dashboard skill row."
    },
    "skill_set_genre": {
      "type": "string",
      "description": "Optional game genre assigned to this skill row."
    },
    "skill_set_order": {
      "type": "number",
      "description": "Display order for the three stacked Luna skill rows."
    },
    "jawan_id": {
      "type": "string",
      "description": "Legacy AI Battle Jawan profile identifier retained for compatibility."
    },
    "jawan_name": {
      "type": "string",
      "description": "Legacy AI Battle Jawan profile name retained for compatibility."
    },
    "jawan_role": {
      "type": "string",
      "description": "Legacy AI Battle combat role retained for compatibility."
    },
    "is_active": {
      "type": "boolean",
      "default": false,
      "description": "Whether this skill loadout row is currently visible/equipped"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [],
      "description": "Loadout tags (PvP, PvE, etc.)"
    }
  },
  "required": [
    "name",
    "equipped_items"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}