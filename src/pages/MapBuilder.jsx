// Placeholder stub — the previous MapBuilder page was removed but the
// dev-server / Tailwind file watcher still references this path. Keeping a
// tiny no-op component here prevents ENOENT errors during CSS compilation.
// Safe to delete once the watcher cache is fully cleared.
export default function MapBuilder() {
  return null;
}